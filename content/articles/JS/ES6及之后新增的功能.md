---
title: "ES6及之后新增的功能"
date: 2026-07-14
category: "JS"
tags: ["JS"]
featured: false
draft: false
readingTime: "1 min"
---
1. let 和 const
2. 对象与数组的解构赋值 
3. 展开运算符{ ...source }
4. 模板字符串
5. 异步处理的演进
   1. Promise
   2. Async / Await
6. 模块化，使用 `import` 和 `export` 取代了 CommonJS 的 `require`
7. 可选链  const city = user?.address?.city;
8. 空值合并运算符 `??`  const count = input ?? 0;
9. 数组新增方法
   1. `find()` / `findIndex()`：查找元素。
   2. `includes()`：判断是否存在某个元素。
   3. `flat()` / `flatMap()`：扁平化数组。
10. Object.keys()
11. Object.assign()
12. for of
13. set
